<UserControl x:Class="BerlieApp.Views.EmployeeListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BerlieApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" FontSize="36">Employee List</TextBlock>

        <DockPanel LastChildFill="True" Margin="0,32,0,0">
            <StackPanel DockPanel.Dock="Top" HorizontalAlignment="Right">
                <Button x:Name="ViewAttachments" Margin="2">View Attachments</Button>
            </StackPanel>
            <Grid DockPanel.Dock="Bottom">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <DataGrid x:Name="Employees"/>
                <GridSplitter Grid.Row="1" HorizontalAlignment="Stretch" Height="5" ShowsPreview="True" ResizeDirection="Rows"/>
                <TabControl Grid.Row="2" x:Name="Items"/>
            </Grid>


        </DockPanel>

    </DockPanel>
</UserControl>
